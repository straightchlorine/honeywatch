# Documentation build dependencies. Kept separate from api/ and ingestor/
# because honeywatch has no root Python package.
#   uv pip install -r docs/requirements.txt && mkdocs serve
mkdocs>=1.6
mkdocs-material>=9.5
mkdocs-git-revision-date-localized-plugin>=1.2
pymdown-extensions>=10.7
