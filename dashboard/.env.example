# API base URL. Empty = same-origin
# Set to http://localhost:5000 for local dev against running API container.
VITE_API_BASE=

# Note: codegen reads `../api/openapi.json`.
# On a fresh clone, run `just openapi-regen` before `pnpm openapi:gen`.
