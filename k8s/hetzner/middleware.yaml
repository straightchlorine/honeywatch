apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: api-ratelimit
  namespace: honeywatch
spec:
  # 60 req/min per source IP, tolerating a 20-req burst. The honeypot's
  # internal nginx keeps its own 30r/s limit as a backstop.
  rateLimit:
    average: 60
    burst: 20
    period: 1m
